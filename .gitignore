# Environment (local only, never commit real values)
.env
.env.*

# Python bytecode (can bake in secrets present at compile time)
__pycache__/
*.pyc

# Agent configuration (local only, not committed)
AGENTS.md
