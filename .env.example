# Local development environment — copy to .env and adjust if needed.
# Defaults below mirror the values in docker-compose.yml (overridable).

SCOPUS_CSV=/app/scopusdata.csv
PKL_MIN_PATH=/app/embeddings_meta_min.pkl
FAISS_PATH=/app/faiss_index_ip.bin
LID_MODEL_PATH=/app/lid.176.ftz

OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=gemma3:4b

PORT=8181
